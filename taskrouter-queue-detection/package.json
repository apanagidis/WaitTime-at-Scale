{
  "name": "taskrouter-queue-detection",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.2.1",
    "eval": "^0.1.6",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.4.0"
  },
  "engines": {
    "node": "12"
  }
}
